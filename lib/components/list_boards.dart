import 'package:flutter/material.dart';
import 'package:todo_app/widgets/boards.dart';

class List_Boards extends StatelessWidget {
  const List_Boards({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container() /*ListView.builder(
        itemCount: 5,
        itemBuilder: (context, index){
          return const Boards();
        })*/;
  }
}
